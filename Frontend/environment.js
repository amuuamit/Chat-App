let IS_PROD = true;
const server = IS_PROD
  ? "https://chat-app-1-2jsj.onrender.com"
  : "http://localhost:5002";

export default server;

// we need to add before edit the programme : http://localhost:5173 instead of deployed link
